# ARCH01 lineage and gate note

The next design work was intentionally not numbered PREP07 or PREP08.

Live repository inspection before ARCH01 creation found existing preparatory work under those identifiers, including:

- `work/animo-prep07-transfer-species-identity`, closed as a qualified source-bound transfer/species identity audit;
- PREP08 branches for restart/state-continuity reservation and causal management probes, with the latter already carrying qualified diagnostic evidence.

ARCH01 therefore uses a separate architecture-series identifier and starts from the qualified extended PREP06 head `9b1f1ea51c24fb82823290193651830dc61ea3c8` rather than overwriting or redefining PREP07/PREP08.

This does not bypass `MIGRATION_DAG.md`. The canonical `STATE` gate remains downstream of `QM`. ARCH01 is only a candidate design prepared from source-bound evidence so that later state-gate qualification can evaluate an explicit architecture instead of starting from an undefined ownership model.
