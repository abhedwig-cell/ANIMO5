# ANIMO-B3D30 — TCD-025-A3 nitrogen restricted ledger admission

Target: `TCD-025-A3`, public N ledger on B3I08-admissible selected intervals.

Base/routing authority: `ANIMO-B3I08@f4a3056087e5f7bac41b243be1f404739cde52b0`. Readiness: `ANIMO-B3A10@872c78b6c020086280e31e1d9a7408e8b5a1afc7`. Aggregate at start remains `ANIMO-RG05J@624bbad35add93de29ac89649155d9fa086a73af`.

The atomic child owns three distinct transported N species identities: DON (`DiorNi`), NH4-N (`Nh`) and NO3-N (`Ni`). Each source-owned storage, boundary-transfer and direct-drain identity must be preserved separately before summing to the public N family. No NH4/NO3 collapse, stoichiometric conversion or residual-derived balancing term is admitted.

The common predicate remains: `for every active D: (I intersection S_D is empty) OR (S_D subset_of I)`. Partial cuts through a saturated mixed macropore reservoir remain outside scope.

No production source, new state, numerical policy, sibling or parent admission is performed. Historical active-macropore B2 evidence is unavailable, so any admission retains historical uncertainty. GOV05 Tier-C review is same-agent and not independent.
