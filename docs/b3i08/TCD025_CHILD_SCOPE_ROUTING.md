# ANIMO-B3I08 — TCD-025 child-scope routing

Base lineage: `ANIMO-B3A10@872c78b6c020086280e31e1d9a7408e8b5a1afc7`, itself based on `ANIMO-RG05J@624bbad35add93de29ac89649155d9fa086a73af`.

B3A10 qualified a restricted public/main macropore-ledger correction surface for selected balance intervals that satisfy the MASSQ03 saturated-reservoir boundary predicate. It explicitly did not qualify the unrestricted parent TCD-025 claim.

B3I08 performs routing only. It does not add science, change the MASSQ03 predicate, admit any child, modify production source, append a new top-level TCD or open B4.

## Child scopes

The restricted correction is partitioned by public observer family so later admissions remain bounded:

- `TCD-025-A1`: water public/main macropore ledger correction on B3A10-admissible selected intervals;
- `TCD-025-A2`: DOM (`DiorMa`) public/main macropore ledger correction on admissible intervals;
- `TCD-025-A3`: N public/main macropore ledger correction on admissible intervals, with DON/NH4/NO3 identities preserved before aggregation;
- `TCD-025-A4`: P public/main macropore ledger correction on admissible intervals, with DOP/PO4 identities preserved before aggregation;
- `TCD-025-A5`: partial saturated-reservoir selected-profile dissolved-species semantics, unresolved and not an admission candidate.

A1-A4 inherit the common B3A10 control-volume contract and exact source ownership. They do not broaden it. In particular, none may admit a balance interval that partially cuts an active saturated mixed macropore species reservoir.

A5 is an explicit blocker scope, not a correction. It prevents child completion from being misread as parent completion while the unsupported selected-profile surface remains open.

## Parent state

The parent remains unadmitted. Completion of A1-A4 must not auto-admit TCD-025. Parent composition requires A5 disposition plus an explicit parent-composition/admission decision.

No new top-level discrepancy number is reserved and the canonical top-level register tail remains `TCD-042`.
