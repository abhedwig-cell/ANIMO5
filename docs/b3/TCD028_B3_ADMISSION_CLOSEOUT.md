# ANIMO-B3D18 — TCD-028 GOV04 Tier-C B3 Admission Closeout

## Decision

`ADMIT_TCD028_ATOMIC_CLASS_B_STABLE_DOM_EVENT_ACCUMULATOR_RESET_WITH_HISTORICAL_UNCERTAINTY_GOV04_TIER_C`

TCD-028 is admitted into B3 for exactly the atomic stable-DOM/DON/conditional-DOP plough-event accumulator lifecycle correction qualified by B3B08, independently reviewed by B3B08R, and formally disposed by B3D17.

The admitted disposition is:

`HISTORICAL_BEHAVIOUR_UNKNOWN_SCIENTIFIC_ADMISSION_WITH_UNCERTAINTY`

This admission does **not** authorize a production patch, B4, corrected-legacy baseline promotion, canonical register mutation, composition, or production migration.

## Admission lineage

Formal disposition authority:

`ANIMO-B3D17@ad1701e72ac51f61f81cc4e9a1857ab14bb8d8d8`

Independent review authority:

`ANIMO-B3B08R@3281f98feef7af4822e85a0f011ede0b779de07b`

Readiness authority:

`ANIMO-B3B08@310d7117da739d19eebb718129ac9494cac854a1`

Current aggregate authority at admission start remains:

`ANIMO-RG05F@7c61a5031f41d602e996310df6f3958cbd1b511e`

The two already-authoritative post-RG05F atomic admissions remain separate sibling authorities and are not merged into this admission branch:

- TCD-030: `ANIMO-B3D15@22e48f7e2c1eaa1245f034de2d909191d9cfa227`;
- TCD-023: `ANIMO-B3D16@8650ea9e716336520d8d7df5f9ea2b393d17ab98`.

## Admitted scientific claim

At each active `Pl(I) > 0` event, `SuStdiorma`, `SuStdiorni`, and conditional `SuStdiorpo` are current-event transaction accumulators. They must begin from the additive identity before the existing current-event stable-pool summation. A stale prior transaction total has no separate physical owner after the prior redistribution has already been materialized in the physical stable-pool state.

The admitted candidate identity is exactly:

```fortran
SuStdiorma = 0.0
SuStdiorni = 0.0
If (Ipo.Eq.1) SuStdiorpo = 0.0
```

inserted at the beginning of the existing `Pl(I)>0` transaction before accumulation. The existing summation domain, redistribution equations, plough trigger, event timing, and conditional phosphorus semantics remain unchanged.

## Risk and historical boundary

B3 qualification class remains `B_LOCAL_ALGEBRA_INDEX_SPECIES`. GOV04 risk remains Tier C because the admitted interpretation depends on initialization/lifecycle semantics and scientific source/state ownership. The independent Tier-C review gate is PASS.

The route is `INDEPENDENT_SCIENTIFIC_ADMISSION_WITH_HISTORICAL_UNCERTAINTY`. GOV03 establishes that a qualified historical B2 reference is unavailable after reasonable acquisition effort. Historical revision-53 Intel/native manifestation therefore remains `UNKNOWN_WITHOUT_B2`. Current-GNU persistence remains diagnostic evidence only.

## Expected differences

The admission permits only differences causally downstream of affected repeated plough events with nonzero stable DOM/DON/conditional-DOP transaction mass. This includes the immediate redistribution state, downstream physical state and process fluxes, dependent C/N/P balances/reports, and restart output only as a downstream image of already changed physical state.

The admission does not change restart serialization or checkpoint representation, numerical precision policy, solver/tolerance policy, unrelated process ordering, management timing, plough triggering, unrelated ledger definitions, frozen B0, or non-plough source semantics.

## Project effect

This is the third atomic B3 admission after RG05F when counted together with TCD-030 and TCD-023. Under GOV04 normal batch policy, the minimum aggregate-integration batch size of three is therefore reached after this admission. Central-regie integration remains a distinct workunit and is not performed here.
