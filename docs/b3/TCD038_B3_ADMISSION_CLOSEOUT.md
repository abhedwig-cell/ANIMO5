# ANIMO-B3D20 — TCD-038 Crop Actual Uptake Restart Initialization Atomic B3 Admission

Decision: `ADMIT_TCD038_ATOMIC_CROP_ACTUAL_UPTAKE_RESTART_INITIALIZATION_WITH_HISTORICAL_UNCERTAINTY_GOV04_TIER_C`.

The qualified state after the exact final B3D20 head passes its own workflow is:

`TCD-038 = ADMITTED_B3_WITH_HISTORICAL_UNCERTAINTY`

This is an atomic B3 admission only. It does not authorize a production patch, canonical STATE promotion, TCD-039 composition, B4, production migration, frozen-B0 mutation, canonical discrepancy-register mutation, or a new RG05 aggregate.

## VERIFY_AND_REUSE basis

B3D20 does not repeat the scientific review. Live checks before branch creation confirmed the existing authorities without a superseding contradiction:

- aggregate: `ANIMO-RG05G@4551b6b4c3f987b1247571d59f8489b2f1a71ba6`;
- readiness: `ANIMO-B3B09@1b47d6b2e422463b557a48355ad8b4f5bed70ebc`;
- independent review: `ANIMO-B3B09R@c6fcd47f4fb4c0277fa27860ee66dc83a449a038`, result `PASS`, run `34528522077`, job `103043260093`;
- formal disposition: `ANIMO-B3D19@8deb1f45c58a9fa3151abc6422aa4f0e55d73a35`;
- B3D19 final-head CI: run `34531475001`, conclusion `success`;
- GOV04: `ANIMO-GOV04@1bbe4c211197590f346803106e45dca5faae79fc`;
- GOV03: `ANIMO-GOV03@cbd262bdabe92923113b7326f2f42822ce9a971c`;
- B3Q01: `ANIMO-B3Q01@846e0f4d02a38b9e02cc1419b1ca87e63aaedb54`.

The current state/routing checks also retained `STATEQ01@4adae99576eb56978da71f7c8a250e4445fd3bc4`, `STATEQ02@cb7c23524df6560e65a5bdc1ed19e0b6e3bd46c6`, `B3I04@400b7cd79f89043e091751707dfa96537587dcf6`, the two extant B3I05 routing heads `1f94a6e08db5d73e8935fb095de9ef9798f6544c` and `7fa0162415e02a6f0167e71b48ae38177a9e06e0`, and `B3I06@8f01f0cb366dfa8cc63a184d6f885100899a8cd9`. None supplies a contradictory or superseding TCD-038 result. STATEQ01/STATEQ02 remain bounded supporting evidence and are not promoted into a TCD-038-specific split-run proof.

No pre-existing B3D20 or later dedicated TCD-038 admission branch was found. The live TCD-038 issue search returned issue #47, the completed independent review with PASS, plus older register-integration issue #17; no newer conflicting TCD-038 scientific evidence was found.

## Exact admitted correction

The B3 qualification class remains `B_LOCAL_ALGEBRA_INDEX_SPECIES`, with GOV04 risk tier `C` because restart/cold-start discrimination, initialization semantics, and checkpoint semantics are involved.

The admitted persistent coordinate is cumulative actual crop uptake. Accepted aliases are `Amplni_act` and, when `Ipo.Eq.1`, `Amplpo_act`. Serialized/result aliases are `Rsamplni_act` and `Rsamplpo_act`.

The admitted restore direction is exactly:

`Rsampl*_act -> Ampl*_act`

The bounded candidate is limited to:

1. `Amplni_act = Rsamplni_act` before the existing nitrogen `1.0d-4` threshold statement;
2. `Amplpo_act = Rsamplpo_act` inside the existing `Ipo.Eq.1` block before the existing phosphorus `1.0d-4` threshold statement.

The existing crop trigger `Kicr(1).Ne.6 .Or. (Kicr(1).Eq.6 .And. Ioptcu.Eq.1)`, the `1.0d-4` rule, and the `Ipo.Eq.1` P guard are retained. No new state or restart discriminator is introduced. The checkpoint representation is unchanged; only the qualified restore semantics are admitted. Potential uptake remains outside scope under TCD-039.

## Cold-start and restart boundary

The shared `INITIAL/>orgpla` initialization surface remains unchanged. B3D20 does not claim a newly identified dedicated cold-start/restart discriminator. Represented values at or below `1.0d-4` keep the existing accepted-input handling and no zero is synthesized.

For the qualified crop trigger, represented serialized actual uptake above `1.0d-4` is admitted to restore into `Ampl*_act` before first process execution. P restoration remains conditional on `Ipo.Eq.1`.

This admission does not generalize into crop checkpoint redesign and does not alter other restore coordinates.

## Historical uncertainty

No qualified B2 authority was found. GOV03 therefore remains controlling for the uncertainty route. Historical revision-53 behavior remains exactly `UNKNOWN_WITHOUT_B2`.

GNU diagnostic behavior is not historical B2. No historical fidelity claim is made. This uncertainty is carried forward with the admission rather than silently resolved.

## Expected differences and non-interference

Differences are allowed only on the qualified causal surface: above-threshold imported cumulative actual N uptake, conditional P uptake, and consequences causally downstream through crop uptake, nutrient demand, nutrient state, fluxes, balances, and outputs.

The admission does not authorize changes to the crop trigger, threshold, P applicability, potential uptake, checkpoint serialization layout, precision policy, solver/tolerance policy, unrelated state/process semantics, or frozen B0 identities. No whole-model equivalence or arbitrary global tolerance is claimed.

Residual uncertainty remains around historical Intel/native manifestation, the absence of a TCD-038-specific causally isolated full continuous-versus-split trajectory, full-horizon magnitude/prevalence of downstream differences, and the separate TCD-039 potential-uptake restart question.

## Admission boundary

B3D20 changes only its own admission evidence, validator, scope guard, status, and workflow. It does not modify `src/`, `reference/`, frozen B0, `docs/quality/THEORY_CODE_DISCREPANCY_REGISTER.csv`, canonical STATE artifacts, RG05 regie artifacts, or production files.

No `RG05H` is created here. Aggregate integration, if ever required later, is a separate central-regie decision.

B3D20 stops after the qualified atomic admission.
