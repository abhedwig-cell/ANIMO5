# ANIMO-B3B08R — Independent Second-Line Review Contract for TCD-028

## Purpose

Perform exactly one genuinely independent second-line scientific review of the frozen `ANIMO-B3B08` admission-readiness object for TCD-028.

This is a GOV04 **Tier C** review because the scientific question includes event-accumulator initialization semantics and event-local versus cumulative source/state ownership. B3 qualification class remains Class B; qualification class and risk tier are distinct.

The review must be executed in a separate ChatGPT context from B3B08 authoring. Separate context establishes process independence only; it does not establish organizational or human independence.

## Frozen review candidate

Readiness authority:

`ANIMO-B3B08@310d7117da739d19eebb718129ac9494cac854a1`

Validated substantive evidence head:

`0145b80e7e308d82995f783cc4febb3b4c9da6ad`

Final readiness workflow:

- workflow: `ANIMO-B3B08 TCD-028 Tier-C readiness`
- run: `34490160286`
- job: `102914502580`
- conclusion: `success`

The readiness validator and atomic scope guard passed on the final readiness head.

## Authorities to recheck live before review

At review start, independently recheck at minimum:

- current aggregate central-regie authority; expected at handoff: `ANIMO-RG05F@7c61a5031f41d602e996310df6f3958cbd1b511e`;
- post-RG05F atomic admissions, including B3D15/TCD-030 and B3D16/TCD-023;
- `ANIMO-GOV04@1bbe4c211197590f346803106e45dca5faae79fc`;
- `ANIMO-GOV03@cbd262bdabe92923113b7326f2f42822ce9a971c`;
- `ANIMO-B3Q01@846e0f4d02a38b9e02cc1419b1ca87e63aaedb54`;
- `ANIMO-B3I01@383c7a83e84a578969f92113280dc715b7bdddb4`;
- `ANIMO-B3I03@814ea660d367494432beb63ea78298d1f6cd73d7`;
- `ANIMO-PREP10@65cd4a65a3ea27cd4ce004f505d5022fdb08b9ab`;
- authoritative `ANIMO-PREP10C@549545921dc8175f01d86c283647acd470c14290`;
- GitHub issue #8 and all current comments;
- any newer TCD-028 branch, issue, evidence, review, corrected candidate or canonical-routing change.

Do not continue on stale pins if the scientific object, canonical identity, governance route or evidence has been superseded.

## Canonical object

Target: `TCD-028`

Canonical process name:

`stable DOM plough redistribution accumulator lifecycle`

Canonical state at the pinned B3I03 register authority: `OPEN`.

The superseded parallel PREP10C proposal that used `TCD-032` is not authoritative for this object and must not be merged or revived.

## Atomic candidate

Routine/member: `ANIMO_4.1.5.53/Addit.for`

Trigger: `Pl(I) > 0`

Frozen Addit SHA-256:

`e3cf8f427e9a4b3743742039c89aae57864cb7268893e45797c7a17427dc8f5d`

Candidate Addit SHA-256:

`a1993aa2d22c8f2c13fc169e78f9121587c8f14a122ef7d3f757cc24a58a54ae`

Candidate current-GNU executable SHA-256:

`f3518bbca36989960fbe02785d64cca873d5e45494baa4e0c0a9a7c026a717db`

Candidate change is exactly:

```fortran
SuStdiorma = 0.0
SuStdiorni = 0.0
If (Ipo.Eq.1) SuStdiorpo = 0.0
```

at the beginning of each active plough event before current-event stable-DOM accumulation.

No other Addit change, state addition, process reorder, numerical-policy change, solver/tolerance change, restart serialization edit, ledger cleanup or composition belongs to TCD-028.

## Mandatory independent questions

The reviewer must reconstruct rather than inherit the following conclusions.

### 1. Frozen identity and exact source seam

Verify source archive, testbank and exact `Addit.for` identities. Independently confirm the `Pl(I)>0` event branch and whether `SuStdiorma`, `SuStdiorni` and conditional `SuStdiorpo` are self-read before explicit event-local definition/reset.

### 2. Event-local versus cumulative scientific ownership

Determine independently whether these three quantities scientifically represent current-plough-event transaction accumulators or intentionally cumulative cross-event storage.

This is the central Tier-C question. Source use-before-definition, GNU persistence and PREP10C author interpretation are not by themselves sufficient. Use source dataflow, redistribution algebra, available theory/documentation and conservation semantics.

If ownership remains scientifically ambiguous, result must not be PASS.

### 3. Conservation identity

Independently verify the plough redistribution control volume:

- `Bo(Pl)` and layer-thickness fractions;
- current-event summation domain;
- redistribution domain;
- whether redistributed fractions sum to one;
- whether a surviving prior-event accumulator would reintroduce prior-event material into a later redistribution;
- whether zero initialization removes exactly the stale prior-event term without deleting current-event mass.

Do not infer correctness merely from a lower balance residual.

### 4. Class and GOV04 risk tier

Reassess both independently.

Expected readiness classification:

- B3 qualification class: `B_LOCAL_ALGEBRA_INDEX_SPECIES`;
- GOV04 risk tier: `C`.

Tier C was selected because `INITIALIZATION_SEMANTICS` and `SCIENTIFIC_STATE_OR_SOURCE_OWNERSHIP_AMBIGUITY` apply. Do not downgrade to Tier B simply because the source edit is three local assignments.

Escalate or fail closed if composition, production-bound scope, new physical state, numerical policy or another stronger trigger appears.

### 5. Causal evidence at original strength

Verify PREP10/PREP10C pins and scope compatibility before reuse.

Expected evidence boundary:

- four successful frozen plough-active cases;
- 31 frozen observed plough events;
- frozen successful cases are non-discriminating because observed accumulators remain zero;
- controlled B0-derived descendants discriminate the reset under current GNU;
- C and N carryover established;
- conditional P carryover established in a phosphorus-enabled case;
- phosphorus-disabled negative control available;
- reset counterfactual changes 12 scientific/structural files in controlled CranMais and 8 in phosphorus-enabled Zuiderzeeland;
- observer non-interference is pinned;
- controlled/synthetic evidence remains B1-style causality, never B2.

The reviewer must not convert current-GNU local-storage behavior into historical Intel/native behavior.

### 6. Candidate non-interference

Verify the PREP10C frozen-testbank comparison and its limitation: eight completing cases, 550 common files, zero scientific/structural differences after declared volatile-only normalization, no missing/extra outputs and no scientific numerical tolerance.

A PASS must explicitly acknowledge that this matrix is supporting non-interference only because those frozen cases are non-discriminating for the defect.

### 7. Historical route

Reconcile the old PREP10C route state with current GOV03.

Expected governance state at handoff:

`B2_REFERENCE_UNAVAILABLE_AFTER_REASONABLE_ACQUISITION_EFFORT`

and:

`ELIGIBLE_HISTORICAL_UNCERTAINTY_ROUTE_SUBJECT_TO_CLAIM_SCOPED_B3_REQUIREMENTS`.

This does not create B2. Historical revision-53 accumulator storage manifestation remains `UNKNOWN_WITHOUT_B2`.

The reviewer must decide whether claim-scoped Tier-C scientific evidence is sufficient despite that historical uncertainty. A PASS may qualify the scientific candidate under historical uncertainty but may not claim historical fidelity.

### 8. Expected-difference surface

Expected allowed downstream differences include stable DOM/DON/DOP redistribution, causally fed physical state, dependent fluxes, balances/reports and restart output/state as a downstream image of changed physical state.

Expected excluded semantics include management event timing, plough trigger definition, restart serialization/checkpoint representation, numerical solver/tolerance policy, precision policy, unrelated ledger definitions and unrelated process ordering.

Any required widening outside that boundary means fail closed or reclassify.

## Independence and result semantics

The review result must be exactly one of:

- `PASS`
- `FAIL`
- `INCOMPLETE`

A PASS means the independent Tier-C scientific review gate is satisfied for this exact frozen readiness object. It does **not** admit TCD-028.

A FAIL or INCOMPLETE must classify the blocker using GOV04 review-failure semantics where applicable and must not create an admission handoff by inference.

## Required persisted output

Persist on this review branch at minimum:

1. a human-readable independent review report;
2. a machine-readable result;
3. exact reviewed readiness and evidence heads;
4. authority and source/evidence pins;
5. explicit Tier-C risk determination;
6. independent event-local ownership and conservation determination;
7. historical-uncertainty boundary;
8. residual uncertainties and expected-difference scope;
9. validation/scope guard appropriate to the review result;
10. explicit statement that no admission, production modification, B4, composition, canonical-register edit or central-regie update occurred.

The authoring context that prepared B3B08 must not write or sign the scientific review result.

## Post-review route

If and only if the independent review returns validated `PASS`, the preferred next governance route is a **separate Tier-C formal-disposition workunit**, followed by a distinct admission decision unless the review explicitly establishes that combining them would not reduce traceability. For this initialization/ownership case, separate disposition and admission should be the default.
