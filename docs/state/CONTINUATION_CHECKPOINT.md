# ANIMO-STATEQ01 continuation checkpoint

This branch now contains a reconciled state-readiness model that:

- consumes PREP12 restart evidence without promoting local TCD labels;
- separates TCD-025 from the independent macropore restart-writer finding;
- separates crop physical stock from continuation-critical potential uptake;
- treats adsorbed NH4 as physical storage but candidate derived checkpoint representation;
- separates layer-0 aqueous state from virtual management/addition reservoirs;
- narrows the first executable checkpoint candidate to `CORE_CNP_SUBSURFACE_ONLY` under a fail-closed no-layer0-activation envelope;
- keeps general `CORE_CNP` blocked by TCD-016-C1;
- defines a restricted-core qualification matrix without granting B3 STATE admission.

Next useful work is source qualification of exact layer-0 activation predicates and management cursor reconstruction. No production code should be written from STATEQ01 before those evidence steps close.
