# ANIMO-B3D32 — TCD-025 restricted parent composition B3 admission candidate

This workunit composes the already admitted bounded children `TCD-025-A1` through `TCD-025-A4` into the supported revision-53 parent claim. It does not widen any child scope and it does not re-open the excluded `TCD-025-A5` surface.

The common interval predicate remains exactly:

`for every active D: (I intersection S_D is empty) OR (S_D subset_of I)`.

The parent is a conjunction/vector of four public/main observer contracts, not a scalar arithmetic sum across unlike families: water, DOM, N and P. N is admitted only after DON, NH4-N and NO3-N source identities remain separate through their child contract. P is admitted only after DOP and PO4-P remain separate through their child contract. No cross-family stoichiometric or atomic-weight conversion is introduced by composition.

`TCD-025-A5` is not scientifically admitted. B3I09 permanently excludes arbitrary partial cuts through a saturated mixed macropore reservoir from the supported frozen-revision53 parent scope because revision53 does not own the necessary internal dissolved-species boundary transfer/state contract. That exclusion is version-bound and may only be revisited by a separate future model-evolution qualification.

The parent admission therefore means only: on intervals satisfying the shared predicate, the four already admitted observer corrections jointly define the supported TCD-025 public/main macropore ledger contract. It is not a whole-model conservation theorem, not historical-fidelity evidence, not production implementation, and not a new physical or numerical model.

GOV04 strictest-trigger logic classifies this non-atomic four-child composition with shared control-volume semantics as Tier D. GOV05 same-agent adversarial review is mandatory and is explicitly not genuinely independent.
