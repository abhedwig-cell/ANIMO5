# ANIMO-B3D29 — TCD-025-A2 DOM restricted ledger admission

Target: `TCD-025-A2`, DOM (`DiorMa`) only.

Base/routing authority: `ANIMO-B3I08@f4a3056087e5f7bac41b243be1f404739cde52b0`. Scientific readiness: `ANIMO-B3A10@872c78b6c020086280e31e1d9a7408e8b5a1afc7`. Aggregate remains `ANIMO-RG05J@624bbad35add93de29ac89649155d9fa086a73af`.

The candidate is bounded to the inherited interval predicate `for every active D: (I intersection S_D is empty) OR (S_D subset_of I)`. A partial cut through a saturated mixed macropore reservoir is not admitted.

Source-owned DOM terms are whole-reservoir old/new storage `SrWaMpOld*CoMpDiorMa -> SrWaMp*RsCoMpDiorMa`, signed unsaturated kernel boundary transfer `TpAm=AvCoML*FlMpVt(next)*St`, and Main-Bypass direct drainage `FlMpOuDrMp*AvCoMpX(1)*St` with `X=DiorMa`. Matrix/macropore exchange and `FlMpOuDrSo` remain internal/routed and are not double-counted as external export.

No production source, state, numerical policy, sibling or parent admission is part of this workunit. Historical active-macropore B2 is unavailable, so any admission retains explicit historical uncertainty. GOV05 Tier-C same-agent adversarial review is mandatory and is not independent.
