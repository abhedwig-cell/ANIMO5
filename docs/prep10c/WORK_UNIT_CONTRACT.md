# ANIMO-PREP10C — Stable-DOM/DON/DOP plough event-reset corrected-legacy candidate

Status: `QUALIFIED_CANDIDATE_REANCHORED_TO_AUTHORITATIVE_PREP10_NOT_ADMITTED`.

## Parent and scope

Authoritative parent branch: `work/animo-prep10-stable-dom-plough-accumulators`

Authoritative parent commit: `65cd4a65a3ea27cd4ce004f505d5022fdb08b9ab`

Authoritative PREP10 decision: `QUALIFIED_GNU_DIAGNOSTIC_CAUSAL_ACCUMULATOR_CARRYOVER_CONFIRMED_CONTROLLED_INPUT_DESCENDANTS_HISTORICAL_INTEL_OPEN`.

PREP10C follows only the authoritative PREP10 lineage. An earlier PREP10C candidate branch was based on the parallel causal branch `work/animo-prep10-stable-dom-causal-activation`; that branch contains useful corroborating evidence but was explicitly reconciled rather than merged into authoritative PREP10. This workunit therefore transplants the candidate-only artifacts onto the authoritative PREP10 head instead of merging the superseded parallel history.

PREP10C creates and qualifies a minimal corrected-legacy **candidate** only. It does not admit corrected legacy, B3 or ANIMO5 production behaviour.

## Frozen identities

- source archive SHA-256: `183c20eb75b6e9f02d33b54aa96fd1537519966401b6b41b9b6b108d98445566`
- testbank archive SHA-256: `44e375510150ff4e9c4f94d81a3b0872aa1c964fefd3a10571c0c2a12b98bb84`
- frozen `ANIMO_4.1.5.53/Addit.for` SHA-256: `e3cf8f427e9a4b3743742039c89aae57864cb7268893e45797c7a17427dc8f5d`
- qualified event-reset counterfactual `Addit.for` SHA-256: `a1993aa2d22c8f2c13fc169e78f9121587c8f14a122ef7d3f757cc24a58a54ae`

Raw frozen artifacts remain outside public Git and are not modified by this workunit.

## Authoritative PREP10 evidence binding

The candidate is evaluated against the following authoritative PREP10 records:

- `integration/animo-prep/PREP10_STABLE_DOM_PLOUGH_ACCUMULATOR_AUDIT.json`;
- `integration/animo-prep/PREP10_GNU_STABLE_DOM_OBSERVER_MATRIX.json`;
- `integration/animo-prep/PREP10_STABLE_DOM_CAUSAL_ACTIVATION_MATRIX.json`;
- `integration/animo-prep/PREP10_PARALLEL_CAUSAL_EVIDENCE_RECONCILIATION.json`;
- `integration/animo-prep/PREP10_STABLE_DOM_B3_HANDOFF.json`.

The parallel evidence reconciliation is supplemental B1 corroboration only and is not a parent lineage.

## Proposed atomic correction

At the start of each `Pl(I) > 0` event, before the stable-DOM/DON/DOP accumulation loop:

```fortran
SuStdiorma = 0.0
SuStdiorni = 0.0
If (Ipo.Eq.1) SuStdiorpo = 0.0
```

No governing equation, state definition, tolerance, precision policy, process order or public interface is intentionally changed.

The candidate is provisionally treated as B3Q01 **Class B: local algebra/index/species correction**, specifically removal of stale event-local accumulator carryover that otherwise duplicates prior-event material in later redistribution. Classification is not admission.

## Central B3 identity

B3 intake has separately reserved `TCD-028` for `stable-DOM plough accumulator event-reset defect` in draft PR #10. The reservation is not yet a canonical register append and the current disposition remains `UNRESOLVED_NOT_ADMITTED`.

PREP10C may cite the reservation, but may not infer canonical registration or admission from it.

## Required evidence and admission boundary

The candidate qualification retains the established requirements: deterministic source identity, byte identity to the causal counterfactual, current-GNU frozen-testbank non-interference, causal convergence, source-bound conservation reconciliation, declared changed/unchanged surfaces, no arbitrary numerical tolerance, explicit B2 status and independent review status.

A provenance-qualified B2 reference remains preferred. The historical-uncertainty route may not be opened unless the documented B2 acquisition effort is genuinely exhausted. Independent second-line review must be separate from candidate authoring.

Therefore a successful candidate test matrix is not corrected-legacy admission, is not B3 admission and does not authorize B4/production migration.
