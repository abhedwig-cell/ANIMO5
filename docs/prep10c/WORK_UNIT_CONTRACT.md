# ANIMO-PREP10C — Stable-DOM plough accumulator reset corrected-legacy readiness

Parent branch: `work/animo-prep10-stable-dom-plough-accumulators`

Parent SHA: `5c927c2f9c218eb740080817203f4962034e5846`

## Purpose

PREP10C converts the qualified PREP10 diagnosis into a fail-closed corrected-legacy readiness contract without starting or admitting a corrected-legacy lineage.

PREP10 has already qualified, for the exact revision-53 source, that `SuStdiorma`, `SuStdiorni` and `SuStdiorpo` are read before explicit event-local definition in the plough branch, and that the missing reset is causally active for controlled B0-derived diagnostic descendants under the current GNU diagnostic contract, including a phosphorus-enabled path.

The existing corrected-legacy governance requires a historical or otherwise independently admitted reference contract before a state-changing corrected-legacy lineage may be admitted. That requirement is not yet satisfied. PREP10C therefore prepares the minimal candidate contract and blocks implementation/admission at the reference gate.

## Frozen identities

- source archive SHA-256: `183c20eb75b6e9f02d33b54aa96fd1537519966401b6b41b9b6b108d98445566`
- testbank archive SHA-256: `44e375510150ff4e9c4f94d81a3b0872aa1c964fefd3a10571c0c2a12b98bb84`
- `ANIMO_4.1.5.53/Addit.for` SHA-256: `e3cf8f427e9a4b3743742039c89aae57864cb7268893e45797c7a17427dc8f5d`

B0 remains immutable and must not be republished in this public repository.

## Candidate defect identity

Proposed discrepancy identifier: `TCD-032`.

Proposed name: `stable-DOM plough event-local accumulator reset`.

Classification: `CONFIRMED_LEGACY_EVENT_TRANSACTION_STATE_DEFECT_CURRENT_GNU_CAUSAL_HISTORICAL_NATIVE_OPEN`.

The defect is distinct from balance-only bookkeeping. The accumulator values participate in redistribution back to `CoStdiorma`, `CoStdiorni` and, conditionally, `CoStdiorpo`, so an erroneous carryover can change later state and process trajectories.

## Minimal candidate semantics

At the beginning of every `Pl(I) > 0` event, before the accumulation loop:

```fortran
SuStdiorma = 0.0
SuStdiorni = 0.0
If (Ipo.Eq.1) SuStdiorpo = 0.0
```

The conditional phosphorus assignment mirrors the conditional use of the P accumulator in the revision-53 branch.

The already qualified PREP10 event-reset counterfactual generated this exact semantic change and has working-copy `Addit.for` SHA-256:

`a1993aa2d22c8f2c13fc169e78f9121587c8f14a122ef7d3f757cc24a58a54ae`

That hash is diagnostic evidence only. It is not a corrected-legacy admission.

## Required admission sequence

A future corrected-legacy workunit may start only after the historical/reference gate for this affected behavior is satisfied or an explicitly governed independent reference substitute is admitted.

After that gate, the candidate must independently demonstrate:

1. exact B0 parent identity before transformation;
2. exact corrected descendant identity and deterministic patch recipe;
3. frozen plough-active testbank noninterference where PREP10 showed the defect dormant;
4. convergence of controlled causal descendants to the already qualified event-reset counterfactual outputs;
5. conservation-ledger reconciliation for C, N and conditional P redistribution;
6. restart/continuation consistency across repeated plough events;
7. no unrelated source changes;
8. a dedicated corrected-legacy admission decision before any ANIMO5 migration use.

## Non-admissions

PREP10C does not:

- create a corrected B0 archive;
- publish proprietary raw source;
- qualify historical Intel behavior;
- admit the diagnostic event-reset transform as corrected legacy;
- admit reference outputs;
- admit ANIMO5 production migration.
