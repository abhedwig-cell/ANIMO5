# ANIMO-KT20 Independent Tier D Review Handoff

Review only the exact frozen KT20 authoring package after exact-final CI succeeds.

Candidate claim:

`EXACT_KT19_NORMALIZED_PACKET_VALUES_CAN_CROSS_THE_NONPRODUCTION_PYTHON_FORTRAN_ADAPTER_SEAM_AND_ENTER_KT18_WITH_FAIL_CLOSED_APPLICATION_SEMANTICS`.

Required review axes:

1. KT19 remains the pinned real-file provider authority and KT20 does not reinterpret SWATRE.UNF bytes.
2. The KT20 frame is explicitly nonproduction and is not presented as a canonical forcing ABI.
3. Every REAL64 crosses the seam through exact IEEE bit identity rather than decimal reformatting.
4. The Python roundtrip recomputes the KT03 typed-step SHA-256 and rejects payload mutation.
5. The Fortran decoder reconstructs exact bits and reuses KT05 validation before KT11.
6. The first real LWKM B1 packet retains typed identity `eeeb862839cce8111535fae86220d8574240804b9f6f029f7ec8e07ceb65da1c`.
7. The real packet is selected through KT11/KT18 but is not claimed to be inside the current admitted TCD-042 envelope.
8. Real-packet application rejection leaves accepted generation, time and science payload unchanged.
9. The synthetic commit control uses the same frame and decoder but remains explicitly nonhistorical.
10. KT18 and KT19 regression suites remain green.
11. No central KT11 authority, B2/B3/B4, canonical forcing, Status A/AA or production claim is introduced.

Same-agent assurance may only be:

`PROCESS_SELF_REVIEWED_NOT_INDEPENDENT`.

A material finding requires remediation, refreeze and a new exact-head CI before any admission decision.
