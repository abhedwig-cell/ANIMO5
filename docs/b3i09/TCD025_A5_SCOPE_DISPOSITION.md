# ANIMO-B3I09 — TCD-025 A5 exclusion and restricted parent-scope routing

This workunit performs governance/routing only. It does not add a new scientific transport law, does not modify production source, and does not admit parent TCD-025.

The frozen revision-53 source does not own an internal dissolved-species transfer/state contract for an arbitrary reporting boundary that cuts through only part of a saturated mixed macropore reservoir. MASSQ03 therefore rejected source closure for that surface and forbade residual-derived completion. B3A10 and B3I08 retained that unresolved surface as TCD-025-A5.

A1 through A4 are now exact-final B3 admissions on the narrower common interval predicate:

`for every active D: (I intersection S_D is empty) OR (S_D subset_of I)`.

B3I09 makes the previously required explicit governance choice: A5 is permanently excluded from the supported parent scope for frozen revision 53. This is a scope disposition, not an A5 scientific admission and not a claim that the missing partial-reservoir physics or observer ownership has been solved.

"Permanent" is version-bound. A future model-evolution workunit may define and independently qualify a new partial-reservoir observer/state contract. Until then, arbitrary partial saturated-reservoir cuts remain unsupported for the composed public water/DOM/N/P parent claim.

The parent may proceed only to a separate restricted composition decision. No automatic parent admission follows from A1-A4 or from this exclusion. The canonical top-level TCD register remains unchanged.
