# TCD-037-A3 Tier-A admission readiness

Work unit: `ANIMO-B3A07`

Target: `TCD-037-A3`

## Readiness conclusion

The exact A3 claim is a low-risk accounting/reporting-only candidate. Readiness is granted only if this workunit's exact-final-head CI remains green. B3A07 does not perform scientific admission and does not grant the final Tier-A waiver.

## Exact accounting owner and observer relation

The source-owned accepted-timestep amount is:

`A_N2Oden(Ln) = QPrN2Oden(Ln) * St`

The existing observer seam applies `Z=10000`, giving the local reporting relation:

`Delta Bani(N2Od,Ly) = 10000 * QPrN2Oden(Ln) * St`

This is a local consumer-seam relation. B3A07 does not turn it into a global theorem about the `Ly`/`Ln` index mapping beyond the qualified source path.

The only allowed observer difference is `Bani(N2Od)`. Nitrification reporting `Banh(N2On)`, atmosphere emission `Bani(N2Oe)`, the `QRdN2O` reduction sink, physical state and GHG process fluxes remain outside A3.

## Causal/coverage evidence

`ANIMO-SYNQ04@31d4ac628edf43501b403f0844dd472bcc12644c` independently qualifies the exact local source/consumer relation with a separate source-shaped Fortran target and exact-rational Python oracle. `ACTIVE_NONZERO`, `ZERO_DENI`, `PERMUTED`, `RATE_TIME_EQUIVALENT` and `INACTIVE` cases pass at O0 and O2 with byte-identical output and no empirical tolerance. Exact-final workflow `34548605202` succeeded.

This remains `B1_SYNTHETIC_NOT_B2`. The natural active-GHG revision-53 case remains `BLOCKED_SOURCE_TESTCASE_LINEAGE_MISMATCH`; no testcase translation was performed. Historical executable behavior therefore remains `UNKNOWN`.

## Tier-A readiness predicate

Under current `ANIMO-GOV05`, GOV04's full Tier-A waiver predicate is retained without weakening. At A3 readiness scope the required conditions are PASS: exact source seam, unambiguous accounting ownership, atomicity, exact accounting identity, predeclared expected-difference surface, physical/process non-interference, no numerical/solver/restart semantics, no composition, no unresolved source-meaning ambiguity, reproducible validator/scope guard, qualified synthetic activation alternative, explicit historical uncertainty, retained GOV03 route, and no production/B4/register mutation.

The readiness result therefore may be `TIER_A_WAIVER_PREDICATE_PASS_AT_READINESS`. The final waiver remains deferred to a later atomic admission decision, which must recheck all authorities and predicates.

## Current aggregate and sibling boundary

Current aggregate authority is `ANIMO-RG05I@94afe7d649a8c60758a41996f0059de0acddd2fc`. A1 and A2 are admitted child atoms; parent `TCD-037`, A3 and A4 are not admitted. B3A07 changes none of those admission states.

## Hard boundaries

No production source or frozen-B0 modification, no canonical-register or routing change, no new top-level TCD, no parent/sibling admission, no TCD-032..036 composition, no B4, no production authorization, no aggregate update, no historical-fidelity claim, no B1-to-B2 promotion and no testcase translation.
