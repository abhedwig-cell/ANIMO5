# ANIMO-BUILDQ02 — B3 governance handoff

Status: `EVIDENCE_STRENGTHENING_ONLY_NO_NEW_TCD_REQUESTED`

BUILDQ02 preserves the B3I01 canonical dispositions already assigned to the two MAPOHYDRO runtime findings.

## Existing TCD-011

The `LnBoMpMx` cross-task lifetime finding remains part of existing `TCD-011`, local storage duration.

New evidence supplied by BUILDQ02:

- state and control-flow materiality under GNU automatic-storage variants is reproduced;
- the required Task-4 value is exactly reconstructable as `Max(LnBoMp(1),LnBoMp(2))` from explicit task arguments;
- a 64-case full Task-1 through Task-4 matrix is byte-identical between original static-local execution and the diagnostic explicit-context candidate across O0, O2, bounds checking and signalling-NaN initialization variants;
- the local is therefore qualified as derivable task context, not persistent physical state.

Requested canonical action:

`STRENGTHEN_EXISTING_TCD_011_EVIDENCE_ONLY`

Do not close or admit TCD-011. Historical Intel project semantics remain unresolved and other storage-duration families remain outside this local qualification.

## Existing runtime hazard without TCD

The MAPOHYDRO bounds/evaluation-order finding remains:

`BUILD_RUNTIME_HAZARD_NOT_TCD`

BUILDQ02 expands its qualified source/runtime scope. In addition to the Task-1 `FrHeWeMpWl(...,0)` searches already known to B3I01, controlled call-order probes now show two Task-2 exposures after unchanged `Modflux` generates `Sqnu=[1,0,2]` with ponding active:

- backward `KEndMpReko` search can evaluate `FlMpInEf(...,0)` at revision-53 line 250;
- `CoStat` classification can evaluate `FlMpInEf(...,0)` at line 264.

Both are confirmed by GNU bounds checking. A diagnostic structural sequencing candidate removes the invalid reads and is byte-identical to the original on the 64-case in-domain matrix.

Requested canonical action:

`EXPAND_EXISTING_BUILD_RUNTIME_HAZARD_SCOPE_NO_TCD`

No evidence currently establishes a distinct scientific equation, state or flux discrepancy under a qualified historical runtime. Do not allocate a new TCD and do not merge this finding with TCD-025.

## Admission state

`new_canonical_tcd_requested=false`

`corrected_legacy_admitted=false`

`production_migration_admitted=false`

`historical_intel_equivalence_qualified=false`
