name: ANIMO B3A08 TCD025 readiness

on:
  push:
    branches:
      - work/animo-b3a08-tcd025-macropore-main-ledger-readiness
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Validate TCD-025 readiness and scope
        run: python tools/validate_b3a08_tcd025.py
