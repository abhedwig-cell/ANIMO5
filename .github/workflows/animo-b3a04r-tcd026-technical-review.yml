name: ANIMO-B3A04R TCD-026 technical review

on:
  push:
    branches:
      - work/animo-b3a04r-tcd026-technical-review
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out full branch history
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Validate persisted technical review
        run: python tools/b3a04r/validate_b3a04r.py
